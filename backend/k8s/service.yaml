apiVersion: v1
kind: Service
metadata:
  name: agri-fi-backend
  labels:
    app: agri-fi-backend
spec:
  type: ClusterIP
  selector:
    app: agri-fi-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
